pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
pip install transformers
pip install tokenizers tqdm numpy requests safetensors
pip install nvidia-cublas-cu12 nvidia-cuda-runtime-cu12 nvidia-cudnn-cu12
pip install accelerate


pip install peft transformers datasets bitsandbytes
pip install --upgrade bitsandbytes peft transformers

pip install fastapi uvicorn transformers
pip install sentencepiece


pip install faker
